import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  item: {
    backgroundColor: '#000',
    padding: 20,
    marginVertical: 8,
    marginHorizontal: 16,
  },
  title: {
    fontSize: 16,
    color: '#fff',
  },
});
